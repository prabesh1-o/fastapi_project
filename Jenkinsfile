pipeline {
    agent any

    stages {
        stage('Checkout Success') {
            steps {
                echo 'Repo cloned successfully'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
